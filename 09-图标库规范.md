# 09 · 液态玻璃图标库规范（LG Icon Library · GoodMom 版）

> 依据 [Semi Icons](https://semi.design/zh-CN/basic/icon)（`@douyinfe/semi-icons`，330+，面性/线性/AI 三套，MIT）的**规范方法**——语义化命名、统一网格、可着色、分类齐全——绘制 **LG 自己的线性图标库**（`lg-icon-*`，77 枚，见 10 类）。
> 定位：系统内一律使用 LG 自绘线性图标（替代 Emoji 与第三方字体图标）；「液态玻璃 3D 图标」为进阶展示形态，见 [03 · 3.8](03-组件规范.md)。

---

## 9.1 与 Semi Icons 的对齐与差异

| 维度 | Semi Icons | LG Icon Library |
| --- | --- | --- |
| 库大小 | 330+ | 77（含 B/L/H；H=儿童健康：五健+口腔+医护检查镜） |
| 风格 | 面性（默认 `IconXxx`）+ 线性（`IconXxxStroked`）+ AI 双色 | **单一线性圆润基线**（与玻璃细线 UI 一致），预留面性/AI 变体 |
| 网格 | 24×24 | 24×24（viewBox） |
| 命名 | PascalCase：`IconSearch` | kebab-case：`lg-icon-search`（便于 CSS/React/SVG 通用） |
| 颜色 | 继承 `color`，双色用 `fill` | `stroke: currentColor` 单色继承；语义色用 Token（见 9.4） |
| 描边 | 两套风格各自统一 | `stroke-width 1.7`，圆头圆接，画在 0.5px 网格 |
| 语义 | 通用图标，不带状态色 | 同款图标可带状态色 Token（绿=成功，红=危险…） |
| 分类 | 按使用场景组织 | 10 类（见 9.3），与组件一一对应 |
| 使用 | React 组件 npm | 内联 SVG / `<symbol>` 雪碧图（本仓库 `icons/lg-icons.svg`） |

> 若项目需复用 Semi 全量图标：`npm i @douyinfe/semi-icons`，选择线性组（`IconXxxStroked`）并按本规范着色即可；**不建议直接引入面性组**（与 LG 细线风格冲突）。

---

## 9.2 绘制标准（Drawing Rules）

```text
画板       24 × 24
内容安全区   20 × 20（四周各留 2px，允许装饰性元素轻微越界 ≤0.5px）
描边       1.7px（图标内细线元素可 1.4–1.6，主轮廓统一 1.7）
端点/连接   round
填充       默认无；仅圆点类元素可用 fill（圆点用圆环 + fill 也可）
网格      所有坐标对齐 0.5 像素；竖线/横线对齐整数
透视       纯正面 2D，禁止伪 3D 立体、渐变描边
圆角       图形内部转角 ≥ 视觉柔和度，避免尖锐内角
```

### 图标形态层级（同组件 L1–L4）

```text
L1 通用线性   → 三级色（--lg-text-tertiary / cool-gray）用于导航、按钮内图标
L2 强调线性   → 紫/青/蓝等语义色（Token）用于状态、KPI、主行动
L3 玻璃底图标 → 白玻璃圆角底 + 线性图标（k-ico / tip 等，3.4 已用）
L4 液态玻璃3D → 进阶展示形态（03.8），用于 Logo 与 Hero，不用于密集控件
```

---

## 9.3 图标清单（77 枚 · 10 类）

> 渲染与复制入口：`preview/index.html`「图标库」区；开发者文件：`icons/lg-icons.svg`（`<symbol>` 雪碧）。

### G · 通用 General（8）
| 名称 | 语义 | 常用组件 |
| --- | --- | --- |
| `lg-icon-search` | 搜索 | 全局搜索、空状态 |
| `lg-icon-home` | 首页/总览 | 导航 |
| `lg-icon-user` | 用户 | 顶栏头像、权限 |
| `lg-icon-bell` | 通知/告警 | 顶栏、徽标 |
| `lg-icon-star` | 收藏/重点 | 列表操作 |
| `lg-icon-grid` | 应用/功能总览 | 导航展开 |
| `lg-icon-layers` | 层级/图层 | 大屏分层 |
| `lg-icon-settings` | 设置 | 侧栏底部、齿轮语境 |

### N · 方向与导航 Direction（7）
| 名称 | 语义 |
| --- | --- |
| `lg-icon-arrow-left / -right / -up / -down` | 移动、翻页、排序方向 |
| `lg-icon-chevron-right` | 展开、进入详情 |
| `lg-icon-chevron-down` | 折叠、下拉 |
| `lg-icon-external-link` | 外链跳转 |

### E · 编辑与操作 Edit & Action（12）
`lg-icon-plus` 新增 · `lg-icon-minus` 减少 · `lg-icon-check` 完成 · `lg-icon-close` 关闭 · `lg-icon-more` 更多 · `lg-icon-edit` 编辑 · `lg-icon-trash` 删除 · `lg-icon-copy` 复制 · `lg-icon-download` 下载 · `lg-icon-upload` 上传 · `lg-icon-refresh` 刷新 · `lg-icon-send` 发送/处置

### S · 状态与提示 Status & Feedback（6）
| 名称 | 语义 | 默认色 |
| --- | --- | --- |
| `lg-icon-info` | 信息 | `--lg-info-500` |
| `lg-icon-warning` | 警告 | `--lg-warning-500` |
| `lg-icon-success` | 成功 | `--lg-success-500` |
| `lg-icon-error` | 错误/危险 | `--lg-danger-500` |
| `lg-icon-help` | 帮助 | cool-gray |
| `lg-icon-clock` | 时间/历史 | cool-gray |

### D · 数据与对象 Data & Objects（10）
`lg-icon-database` 数据库 · `lg-icon-server` 服务器 · `lg-icon-cloud` 云 · `lg-icon-file` 文件/文档 · `lg-icon-folder` 目录/资产 · `lg-icon-chart-bar` 柱状图 · `lg-icon-chart-line` 趋势图 · `lg-icon-chart-pie` 占比图 · `lg-icon-network` 网络拓扑 · `lg-icon-globe` 区域/全球

### A · 安全与 AI Security & AI（7）
| 名称 | 语义 | 默认色 |
| --- | --- | --- |
| `lg-icon-shield` | 防护 | `--lg-purple-500` |
| `lg-icon-shield-check` | 防护已启用 | `--lg-success-500` |
| `lg-icon-lock` | 锁定 | cool-gray / 危险时 danger |
| `lg-icon-key` | 密钥/凭证 | `--lg-warning-500` |
| `lg-icon-eye` | 可见/审计 | cool-gray |
| `lg-icon-ai-spark` | AI 智能 | `--lg-purple-500` |
| `lg-icon-ai-bot` | 智能体 | `--lg-purple-500` |

### M · 媒体与展示 Media & Display（6）
`lg-icon-image` 图片 · `lg-icon-play` 播放/运行 · `lg-icon-calendar` 日历/排期 · `lg-icon-pin` 定位 · `lg-icon-monitor` 大屏/终端 · `lg-icon-list` 列表/事件流

### B · 母婴与保健 Maternal & Baby（6，妈咪好 GoodMom 专类）
| 名称 | 语义 | 默认色 | 对应领域（[13](13-妇幼领域组件规范.md)） |
| --- | --- | --- | --- |
| `lg-icon-baby` | 宝宝 | Rose-500 | 宝宝首页 / 档案 |
| `lg-icon-mom` | 妈妈关怀 | Rose-500 | 妈妈端 / 爱心服务 |
| `lg-icon-milk` | 喂养 / 母乳 | Peach-400 | 喂养记录（13.3.7） |
| `lg-icon-vaccine` | 疫苗 | Mint-500 | 疫苗计划卡（13.3.3） |
| `lg-icon-vital` | 胎心 / 健康 | Rose-600 | 孕周卡 / 生命体征 |
| `lg-icon-clinic` | 儿保机构 | Mint-500 | 机构端 / 体检 |

### L · 成长阶段 Stage & Growth（8，孕妇阶段 + 儿童年龄段）

| 名称 | 语义 | 对应阶段（15.2.10 / 12.2.1） |
| --- | --- | --- |
| `lg-icon-preg-prep` | 备孕 | 备孕期（无孕肚 + 爱心） |
| `lg-icon-preg-early` | 孕早期 | 孕早期（孕肚较小） |
| `lg-icon-preg-mid` | 孕中期 | 孕中期（孕肚增大） |
| `lg-icon-preg-late` | 孕晚期 | 孕晚期（孕肚最大） |
| `lg-icon-child-newborn` | 新生儿 | 0–28 天（襁褓） |
| `lg-icon-child-infant` | 婴儿 | 1–12 月龄（坐姿） |
| `lg-icon-child-toddler` | 幼儿 | 1–3 岁（站立小个） |
| `lg-icon-child-preschool` | 学龄前 | 3–6 岁（身高增长 + 小书包） |

### H · 儿童健康 Child Health（7 · 综合儿保五项 + 口腔，含医护检查镜）

| 名称 | 语义 | 对应业务项（15.2 / 13 章） |
| --- | --- | --- |
| `lg-icon-wj-growth` | 生长发育 | 身高体重测量 · 生长曲线（P3–P97） |
| `lg-icon-wj-nutrition` | 营养喂养 | 喂养指导 / 辅食 · 营养评估 |
| `lg-icon-wj-develop` | 心理行为发育 | 发育筛查 / 心理行为评估 |
| `lg-icon-wj-vision` | 视力保健 | 眼保健 / 视力筛查 |
| `lg-icon-wj-hearing` | 听力保健 | 听力筛查 / 耳保健 |
| `lg-icon-wj-oral` | 口腔保健 | 口腔检查 / 龋齿防治（涂氟、窝沟封闭提示） |
| `lg-icon-wj-oral-scope` | 口腔检查（医护版） | 口腔检查镜 · 医生接诊 / 检查记录 |

> 绘制说明：生长发育=身高测量人物 + 双向箭头；营养=苹果；心理=笑脸 + 星芒（正向发育）；视力=眼睛 + 视标 E；听力=头戴耳机。
> 该五健图标用于儿童档案五健记录、园所体检清单、儿保套餐 Tab。

> 绘制规则：孕肚以「头部圆 + 肚部圆」随阶段增大；儿童以头身比与身高增长区分。新增阶段图标须按 09.6 流程登记后扩展。

---

## 9.4 使用规范

### 尺寸档位

```text
sm 16px   行内、徽标内        stroke 1.6
md 20px   按钮/导航默认        stroke 1.7
lg 24px   空状态、卡片头部     stroke 1.7
xl 32–40px  大屏状态图标      stroke 1.8
```

### 颜色规则

```css
/* 默认继承文字色： */
.lg-icon{ color: var(--lg-text-tertiary); }
.lg-icon:hover{ color: var(--lg-purple-600); }
/* 语义状态：直接用状态 Token 包一层 color */
.icon-danger{ color: var(--lg-danger-500); }
```

```text
1. 图标默认取 currentColor；禁止在 SVG 内部硬编码颜色（双色/AI 变体除外）。
2. 同一语义全场同色：所有"删除"用 danger、所有"成功"用 success（04.5 色彩一致性同款规则）。
3. 主行动按钮内图标用白色（按钮自身紫渐变），禁止再加一层彩色。
4. 图标不承载颜色语义时用三级色，让数据与文字更亮。
```

### 布局与可点区域

```text
- 图标与文字间距 8px（按钮内 6–8px，对齐文字基线 ±2px）。
- 独立可点图标 ≥44×44 热区（视觉 20–24 居中；桌面鼠标场景可降至 40）。
- 列表/表格内图标 16–18px，行高内垂直居中。
```

---

## 9.5 开发者接入

### 方式 A：内联 SVG（推荐单枚使用）

```html
<svg class="lg-icon" viewBox="0 0 24 24" aria-hidden="true" width="20" height="20">
  <path d="…" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round"/>
</svg>
```

### 方式 B：雪碧图 `icons/lg-icons.svg`

```html
<svg class="lg-icon" width="20" height="20"><use href="icons/lg-icons.svg#lg-icon-search"/></svg>
```

### 方式 C：前端组件库
把 `lg-icons.svg` 的 `<symbol>` 转成 React/Vue 组件（svgr 同 Semi 文档），命名 `LgIconSearch`（PascalCase 由 kebab 自动转换）。

---

## 9.6 新增图标流程（与 Semi 一致的提交流程）

1. 先查 9.3 清单，确认无同语义图标（避免重复）。
2. 按 9.2 标准在 24×24 画板绘制，仅线性、无色。
3. 命名 `lg-icon-{语义英文kebab}`，加入对应分类表 + 雪碧图 + 预览页。
4. 语义色通过 Token 控制，不在图标内部写死。
5. 交由「9.7 验收」评审后合并。

---

## 9.7 图标验收清单

- [ ] 24×24 画板、内容在 2px 安全区内、坐标对齐 0.5
- [ ] 描边 1.7 圆头统一，无渐变/无填充（除圆点）
- [ ] 无伪 3D、无 Emoji 化、无锋利内角
- [ ] 同屏图标线宽与圆角一致（1 套基线）
- [ ] 命名语义唯一、kebab-case、已加入清单
- [ ] 尺寸档与组件配套（16/20/24/32）
- [ ] 默认无色（currentColor），语义色走 Token

---

> 参考：[Semi Design Icon 文档](https://semi.design/zh-CN/basic/icon) · [@douyinfe/semi-icons (MIT)](https://www.jsdelivr.com/package/npm/@douyinfe/semi-icons) · [Semi 组件总览](https://semi.design/zh-CN/start/overview)
